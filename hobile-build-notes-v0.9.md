# Hobile Online — סטטוס בנייה (v0.9)

תוצר: ריפו מלא ב‑`src/` (מקור קריא, 15 מודולים), שרת Colyseus אמיתי, דמו בקובץ אחד `dist/solo.html`, וארטיפקט מפורסם שאפשר לשחק בו מהנייד.

## v0.9 — שחזור הקוד, השרת, והבאגים שהתגלו בדרך

### הזיפ אבד — הקוד שוחזר מהארטיפקט המפורסם
הריפו היה קיים רק כזיפ אצל מאיר. השחזור נעשה מהבנייה המפורסמת, והתברר שהוא אפשרי במלואו כי **שמות מתודות ומאפיינים שורדים מינפיקציה** — רק משתנים מקומיים ושמות ברמה העליונה עוברים ערבול.
- `tools/recovery/split.mjs` — rename מודע‑scope ב‑Babel ל‑326 סמלי משחק, ועוד **71 מחלקות three.js שזוהו אוטומטית** לפי התגים ש‑three משאירה על עצמה (`this.isVector3 = !0`, `this.type = "SphereGeometry"`). אחר כך פיצול ל‑15 מודולי ES שה‑imports שלהם נגזרים מגרף ההפניות.
- כל הדאטה שרד שלם: 33 מינים עם רשומות `DESIGN`, 33 מהלכים, 10 יסודות, 8 אזורים, 4 מבוכים, משימות, לוחות זמנים ודיאלוג NPC, מתכונים, גילדות, בוסים, ומחרוזות ה‑UI בעברית.
- העברית בקוד היא עברית, לא `א`. three.js מגיע מ‑npm במקום עותק מוטבע.
- **`tools/recovery/` הוא היסטורי. אסור להריץ אותו** — `split.mjs` מוחק את `src/` ובונה מחדש מהבנדל, כלומר מוחק כל תיקון שנעשה מאז. `src/` הוא מקור האמת.

### קרב הצוות היה מת מקצה לקצה
`docs/battle-v2.md` מתאר צוות שנלחם ומאמן שמאחוריו. ארבעה פערים נפרדים מנעו מזה להגיע לשחקן:
1. **`BattleSim` הוסיף רק את היצור הפעיל.** לא היה ספסל להחליף אליו, ולא היה מאמן ש‑`enemiesOf` יחשוף כשהצוות נופל. עכשיו נכנס כל הצוות עם `slot`, ועוד המאמן.
2. **`battleInit` לא שלח `team` ולא `trainer`** — שני שדות שהקליינט קורא.
3. **`syncBattleState` לא העתיק `benched` / `slot` / `frozenUntil`.** הקליינט ראה `benched=false` ו‑`slot=undefined` לכולם.
4. **כפתור ההחלפה שלח הודעה שאף חדר לא טיפל בה.** נוסף `swapToUid` שממפה uid של יצור לקומבטנט **בצד של המבקש** (בלי בדיקת הבעלות אפשר להחליף יצור של חבר קבוצה, וב‑PvP של היריב).

### באגים נוספים שתוקנו
| באג | סיבה |
|---|---|
| **חמשת העופות עפו עם רגליים תלושות** | כל design קבע `hipY` משלו, ובכולם הוא היה מתחת לבטן. הירך נגזרת עכשיו מהגוף, הרגל מגיעה מהירך לקרקע, ו‑haunch מחבר ביניהם. |
| **הכנפיים נעלמו מרוב הזוויות** | `finProfile` הוא יריעה בעובי אפס וכל השכבות חלקו מישור אחד. זה בדיוק לקח הרעמה: יריעה מהצד היא קו של שני פיקסלים. כל נוצה היא עכשיו אליפסואיד שטוח, עם קנה כהה בשפה הקדמית. |
| **`swapCreature` מוגדר פעמיים** באותו אובייקט hooks, ו‑**`bestSphere` פעמיים** באותה מחלקה | ההגדרה השנייה דרסה בשקט את הראשונה. |
| **שלוש משימות העניקו פריטים שלא קיימים** | `mat_scrap_iron` / `mat_fiber` / `mat_aether_core` מול `scrap_iron` / `fiber` / `aether_core` האמיתיים. `giveItem` החזיר false והפרס נעלם. |
| **השרת האמין לכל מיקום שהקליינט שלח** | אין clamp על `move`. הקירות עצרו רק את מי שבחר ללכת אליהם. עכשיו 3 מטר לחבילה. |

### השרת
- `WorldRoom` — חדר לכל אזור, יצורי בר משותפים עם שיטוט בצד השרת, חלונות בוס עם טבלת תרומה, צ'אט אזור/גילדה/קבוצה.
- `BattleRoom` / `DungeonRoom` — מריצים את **אותו** `BattleSim`/`DungeonSim` של מצב שחקן יחיד, דרך adapter בצורת ה‑store הלוקלי.
- **פרוטוקול אחד לשני הצדדים**: 34 הודעות העולם יצאו מ‑`WorldSim` ל‑`src/server/game/world-messages.js`, שנכתב מול context קטן ששני הדרייברים מספקים. שינוי איזון לא יכול לנחות באחד ולפספס את השני.
- API: register / login / guest / me / character / leaderboard / guilds. scrypt ל‑סיסמאות, HMAC לטוקנים, `node:crypto` בלבד.
- אחסון מאחורי ממשק אחד: זיכרון (ברירת מחדל, אופציונלית עם קובץ) או MongoDB ב‑import דינמי.

### מלכודות colyseus 0.16 / schema 3 (עלו זמן)
- **האימות מגיע ב‑join options** אצל הקליינט הזה, ולכן החדרים משתמשים ב‑`onAuth` של המופע ולא בסטטי.
- **`ArraySchema#splice` לא יכול להכניס יותר ממה שהוא מוחק** — מערכים מתנקים ומתמלאים מחדש.
- schema v3 לא יוצר קולקציות לבד: כל `MapSchema`/`ArraySchema` חייב קונסטרקטור מפורש.
- `@colyseus/core@0.16.25` שבור (תלות `workspace:^`). הגרסאות המוצמדות: core 0.16.24 · ws-transport 0.16.5 · schema 3.0.76 · colyseus.js 0.16.22 · three 0.185.1.

## בדיקות
| פקודה | מה נבדק |
|---|---|
| `npm run qa` | תקינות דאטה, עקומת הלכידה (5/51/99), גבולות אזורים, חוזה battle‑v2 — **58 בדיקות** |
| `npm run test:server` | מרים שרת אמיתי ומחבר שני קליינטים אמיתיים: רפליקציה, ה‑clamp, צ'אט, מעבר לקרב, שמירה — **22 בדיקות** |
| `npm run test:browser` | דפדפן אמיתי: יצירת דמות, כניסה לעולם, אפס שגיאות קונסול, תקציב רינדור |
| `npm run shot` | צילום מסך אחד — הדרך היחידה לבדוק איך יצור *נראה* |

תקציב: ≤280 draw calls, ≤420k משולשים. בפועל: **74 / 190k**.

## פריסה
- קליינט: `npm run build:web` → `dist/web/`.
- שרת: **לא Vercel** — max duration סוגר חיבורים, אין instance affinity, וחדר Colyseus חי בזיכרון של תהליך אחד. Railway / Render / Fly.io / VPS. `Dockerfile` ו‑`railway.json` מוכנים.
- לפני עלייה לאוויר: `AUTH_SECRET` ארוך ואקראי · `CORS_ORIGIN` מוגבל · `DB_DRIVER=mongo`.

## מה נשאר
- **`DESIGN` ממופתח לפי id של המין. מין חדש ב‑`SPECIES` בלי רשומת `DESIGN` נופל לגוף משותף ונראה כמו שיבוט.**
- ארבעה יצורים עדיין חלשים ויזואלית: `frostnib` (שק כהה), `glacilisk` (נקניק אופקי), `duskmaw` (מסה כהה), ו‑`aurorix` — שהוא הלג'נדרי ואמור להיות המרשים במשחק.
- פנים‑מבנה: אותם ארבעה ריהוטים לכל דלת.
- PvP, מסחר ובית מכירות, דירוג עונתי, מזג אוויר, glTF במקום מודלים פרוצדורליים, בסיס כמפה תלת‑ממדית.
