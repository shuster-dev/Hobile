# Assets

V1 deliberately uses generated primitive geometry and CSS UI, so there are no copied textures, models, weapon icons, music or sounds.

Future original Hobile assets should live here under clear subfolders such as `models/`, `textures/`, `audio/` and `ui/`.
